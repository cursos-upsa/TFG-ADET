const ForumContent = ({doubts}) => {

    if (doubts.length === 0) {
        return <p>Aún no hay contenido en este foro.</p>
    }

    return (
        <ul>
            {/* TODO */}
        </ul>
    );
};

export default ForumContent;